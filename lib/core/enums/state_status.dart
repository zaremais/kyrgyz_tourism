enum StateStatus { init, loading, success, failure }

enum TourFilter { best, oneDay }

enum MessageType { text, image, file }

enum TourDifficulty { easy, medium, hard }

enum TelegramStatus { initial, loading, success, failure }
